import Box from "@mui/material/Box";

const SideBar = () => {
  return (
    <Box
      sx={{
          width: 'auto',
        height: '100vh',
        backgroundColor: "primary.dark",
      }}
    />
  );
};

export default SideBar;
