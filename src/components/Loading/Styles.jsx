import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
    loading:{
        width: '100%',
        height: '100%',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center'
    }
})